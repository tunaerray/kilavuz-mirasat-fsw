[pytest]
# Proje kökünü import yoluna ekler (src.* ve config.* paketleri için).
pythonpath = .
testpaths = tests
addopts = -q
# pytest-timeout mevcutsa global koruma (sonsuz döngü/asılı test önlemi).
timeout = 30
